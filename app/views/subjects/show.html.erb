<% @page_title = @subject.name %>

<div class="row">

  <div class="col">
  </div>

  <div class="col-8">
<%= link_to("Back to Subjects", subjects_path, :class => 'back-link') %><br /><br />

  <h2 style="margin-bottom: 30px;">Show Subject</h2>

  <table summary="Subject detail view"  class="table table-borderless">
    <tbody>
    <tr>
      <th>Name</th>
      <td><%= @subject.name %></td>
    </tr>
    <tr>
      <th>Position</th>
      <td><%= @subject.position %></td>
    </tr>
    <tr>
      <th>Visible?</th>
      <td><%= @subject.visible ? 'true' : 'false' %></td>
    </tr>
    </tbody>
  </table>
  </div>
  
  <div class="col">
  </div>

</div>
